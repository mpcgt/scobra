<?xml version="1.0" encoding="UTF-8"?>
<svg width="224px" height="30px" version="1.1" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink" onload="init()">
    <title>Count</title>
    <g id="Page-1" stroke="none" stroke-width="1" fill="none" fill-rule="evenodd">

        <!-- Vos rectangles et textes ici... -->

        <script type="text/ecmascript">
            <![CDATA[
                function init() {
                    // Ajoutez votre logique JavaScript ici
                    var counters = [0, 0, 0, 4, 3, 5, 7]; // Exemple de compteurs initiaux pour chaque appareil

                    // Mettez à jour les textes avec les valeurs des compteurs
                    for (var i = 0; i < counters.length; i++) {
                        var textElement = document.getElementById(i.toString());
                        if (textElement) {
                            textElement.firstChild.textContent = counters[i];
                        }
                    }
                }
            ]]>
        </script>

    </g>
</svg>